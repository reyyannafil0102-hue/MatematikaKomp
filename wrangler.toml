# This project deploys as a static Cloudflare Pages site.
# The correct deploy command is:
#   npm run deploy
#
# If you use `npx wrangler deploy`, it will attempt a Worker deploy and fail
# because this project is configured as a Next.js static export.

name = "fungsi-komposisi"
pages_build_output_dir = "./out"
